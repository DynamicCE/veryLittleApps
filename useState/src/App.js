import React, { useState } from "react";
// we used to import React from "react" before, but if we are going to use states, we must import {useState} too
export default function JustAComponent(props) {
  return (
    <div>
      hello
      {/*here is jsx, i must these curly brackets and the other signs for description lines */}
    </div>
  );
}
